# nacos 配置
spring:
  application:
    name: gulimall-product  # 服务名
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.100.128:8848  # Nacos 注册中心地址
      config:
        server-addr: 192.168.100.128:8848  # Nacos 配置中心地址
        file-extension: yaml         # 远程配置文件的后缀